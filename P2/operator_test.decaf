
class Program {
    bool x, y, z, res;
    int n, m, v, num;

    static void main() {
        if ( x && !y || x && y >= z ) {
            res = false;
        }
        if ( (n != m) && !( v >= n ) || ( m == n ) ) {
            num = x + y % 5 * 44;
        }
    }
}

function( ab, ( y > 1 ), false, 23.4 )