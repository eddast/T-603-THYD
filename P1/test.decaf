/* multi
line
comment*/

// Singleline comments

class Program {
    int i, j;
    real number;
    bool hehe = false;

    static void main() {
        i = 0;
        j = 1;
        k = 22;


        _jkjs99;
        _09383;
        jkdjld09902_
        edda_829892_jesklhoehe9938939_i93803jhks

        03_edda &


        if ( i < j ) {
           number = 12.34;
        }
        /* this multiline comment contains some weird symbols! % # $ &7819*__ 2"
            and also some newlines!
            */
        else {
           number = 12.34E+7;
           number = 12.3E-BB;
           12BBEE;
        }
        i = i * j;

         public double slopeTo(Point that) {
            // (y1 − y0)/(x1 − x0)

            // Degenerate line (between point and itself)
            if(xequals && yequals) {
                return Double.NEGATIVE_INFINITY;
            }
            // Vertical line
            if(xequals) {
                return Double.POSITIVE_INFINITY;
                head.setData()
            }
            // Horizontal line
            if(yequals) {
                return 0;
            }

            class Example {
                void main() {
                    int x; int y;
                    x = read();
                    y = read();
                    print(gcd(x, y));
                }
                int gcd(int a, int b) {
                    if ( b == 0 )
                        hehe = !hehe;
                        return a;

                    else if ( j != k ) {
                        return k;
                    }
                    else
                        return gcd(b, a % b);
                }
            }


            return ((double) y)/x;
        }
    }
}